---
import Title from "./Title.astro";
interface Props {
  name: string;
}
const { name } = Astro.props;
---

<section id={name} class={` relative w-full h-fit  grid  px-2 `}>
  <Title title={name} />
  <slot />
</section>
